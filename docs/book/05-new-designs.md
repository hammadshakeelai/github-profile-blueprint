# 5 · New designs — current trends, built to survive a phone

Chapter 1 found that every beautiful profile hero we measured puts its smallest
text between 2.2 and 5.2 pixels on a phone. This chapter is the answer: nine of
the design trends that dominate the web in 2026, each rebuilt as an original,
README-safe SVG that you could ship today.

Every piece here, by construction:

- **clears the legibility floor** — drawn on a 600-unit canvas where no label is
  smaller than 21.4 units, so nothing drops below 11px in the 309px column a phone
  gives a README. The generator *refuses* to draw smaller text, and refuses text
  that would run off the canvas;
- **is finished at frame zero** — motion only adds, so screenshots, previews and
  paused tabs see the whole thing;
- **has a dark and a light file**, switched by `<picture>`;
- **uses only what renders in all three engines** — no `gradientTransform`
  animation, no scripts, no external images, no web fonts.

Generated by [`tools/book/designs.py`](../../tools/book/designs.py) from the same
data as the rest of the book. Change the name at the top and rebuild.

They are also small: **all nine together weigh about 40 KB** (1.6–17 KB each) —
less than a quarter of a single hero from chapter 1, where 162 KB, 203 KB and
1.4 MB files turned up.

---

## Aurora

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="designs/aurora-dark.svg">
  <img src="designs/aurora-light.svg" width="600" alt="Hammad Shakeel: aurora light drifting behind the name, with film grain">
</picture>

Soft coloured light drifting behind the content — the most bookmarked background
of the last two years. Four circles, each drifting on its own period, under one
heavy `feGaussianBlur`; then a full-canvas `feTurbulence` layer at 9% opacity for
**film grain**, which is what stops a gradient looking cheap.

*Use it for:* a hero. *Weight:* ~2 KB.

## Liquid glass

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="designs/liquid-glass-dark.svg">
  <img src="designs/liquid-glass-light.svg" width="600" alt="Hammad Shakeel on a pane of liquid glass that refracts moving colour behind it">
</picture>

The glass pane *bends* the colour moving behind it. The backdrop is drawn twice:
once plainly, once inside the pane through `feTurbulence` → `feDisplacementMap` →
a little blur, which is real refraction rather than a frosted tint. A gradient
stroke gives the specular rim. Seen in the wild on
[XxMasterepicxX](https://github.com/XxMasterepicxX) (as ink) and
[Umeem26](https://github.com/Umeem26) (as glass).

We tested whether these filters survive inside an image in every engine — see
[verified](#verified). They do.

## Bento

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="designs/bento-dark.svg">
  <img src="designs/bento-light.svg" width="600" alt="A bento grid: Hammad Shakeel, top languages, 87 repositories, 28 live sites, what's being built now, latest repositories">
</picture>

Modular tiles of different sizes — the layout of 2026. Every bento generator we
found ([BentoHub](https://github.com/amittam104/BentoHub),
[OP Bento](https://opbento.vercel.app/)) builds it from an HTML table, and the
survey measured what tables do on a phone: cards squeezed to 76px or a table that
scrolls sideways. **Drawing the whole bento as one image** keeps the grid intact;
it simply scales. Real numbers, one pulsing dot.

## Neo-brutalism

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="designs/brutal-dark.svg">
  <img src="designs/brutal-light.svg" width="600" alt="HAMMAD SHAKEEL in neo-brutalist style: a yellow card with a hard black shadow and three flat tags">
</picture>

Thick outlines, a hard offset shadow with no blur, flat loud colour, condensed
capitals. The card nudges into its shadow and back, like a pressed button. The
cheapest style here to render and one of the loudest.

## Kinetic type

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="designs/kinetic-dark.svg">
  <img src="designs/kinetic-light.svg" width="600" alt="The name SHAKEEL in heavy capitals, each letter swelling in weight in turn">
</picture>

Variable fonts can't be loaded inside an image, so this fakes a weight axis: each
letter is a `<tspan>` whose stroke, in the fill colour, swells and relaxes, one
letter after another. One `<text>` element, so the font's own spacing is kept.

## Holographic chrome

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="designs/chrome-dark.svg">
  <img src="designs/chrome-light.svg" width="600" alt="SHAKEEL as an iridescent chrome wordmark with a slowly sweeping highlight">
</picture>

An iridescent gradient fill with a slow sweep and a specular top-light layered
over it. The sweep animates the gradient's `x1`/`x2`, **not** `gradientTransform`
— WebKit never animates the latter, so a chrome sweep built that way is frozen for
every Safari visitor.

## Isometric city

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="designs/isometric-dark.svg">
  <img src="designs/isometric-light.svg" width="600" alt="An isometric city of 24 towers, one per month, height proportional to repository activity">
</picture>

The same real 24 months as the [3D model in chapter 4](04-native.md#an-interactive-3d-model),
drawn as isometric towers: three polygons per tower, painted back to front. Isometric
contribution art descends from [jasonlong's browser extension](https://github.com/jasonlong/isometric-contributions)
(3,700★); this version is a static file with no Action.

## Blueprint

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="designs/blueprint-dark.svg">
  <img src="designs/blueprint-light.svg" width="600" alt="A blueprint drawing with dimension lines stating 600 units equals 309 pixels, and a title block">
</picture>

A technical drawing: a patterned grid, dimension lines, a title block, and a frame
that draws itself once. The dimension states the rule this whole chapter is built
on — 600 units equals 309 pixels.

## Halftone

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="designs/halftone-dark.svg">
  <img src="designs/halftone-light.svg" width="600" alt="A red halftone dot screen that swells toward the right, with the word HALFTONE">
</picture>

A print screen in vector: dot radius falls off with distance from a focal point.
The honest cousin of [ayxn07](https://github.com/ayxn07)'s dithered photo — no
raster, and it themes.

---

## Verified

**The filters.** Liquid glass and film grain depend on `feTurbulence` and
`feDisplacementMap`, which this repository's [capability matrix](../CAPABILITY-MATRIX.md)
had not tested. [`tools/book/filters.mjs`](../../tools/book/filters.mjs) renders a
test scene as an `<img>` with and without each filter and compares screenshots, the
method that settled the [reduced-motion question](../research/REDUCED-MOTION.md):

| | Chromium 153 | Firefox 155 | WebKit 26.6 |
|---|---|---|---|
| `feTurbulence` | applied | applied | applied |
| `feDisplacementMap` | applied | applied | applied |
| `feGaussianBlur` (control) | applied | applied | applied |

**The layout.** Every file passes the generator's two checks — no label under 21.4
units, nothing off the canvas — and was looked at, in both themes, before publishing.
Five defects were found by eye and fixed: uneven letter spacing in the kinetic piece,
backdrop circles spilling past the glass card, a repository name cut mid-word, a
caption colliding with the halftone dots, and an off-centre city.
