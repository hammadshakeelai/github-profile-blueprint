{
  "name": "edge-telemetry-worker",
  "version": "1.0.0",
  "description": "Serverless edge SVG generator for real-time GitHub Profile telemetry with Camo cache bypass",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
